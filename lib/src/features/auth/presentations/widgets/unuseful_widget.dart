import 'package:flutter/material.dart';

class UnusefulWidget extends StatelessWidget {
  const UnusefulWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Example Widget'),
    );
  }
}
